'use strict';

describe('myApp.resume module', function() {

  beforeEach(module('myApp.resume'));

  describe('resume controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resumeCtrl = $controller('ResumeCtrl');
      expect(resumeCtrl).toBeDefined();
    }));

  });
});